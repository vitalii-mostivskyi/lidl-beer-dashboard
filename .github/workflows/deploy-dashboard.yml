name: Deploy Beer Dashboard

on:
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare site directory
        run: |
          mkdir -p site
          cp -R BeerDashboard/* site/

      - name: Copy committed JSON from repo root
        run: |
          if [ -f untappd-beers.json ]; then
            cp untappd-beers.json site/untappd-beers.json
          else
            echo "ERROR: untappd-beers.json not found at repo root. Commit the file and re-run the workflow." >&2
            exit 1
          fi

      - name: Deploy to GitHub Pages via gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site
